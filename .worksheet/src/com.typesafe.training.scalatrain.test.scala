
package com.typesafe.training.scalatrain

import TestData._
import java.lang.{ IllegalArgumentException => IAE }
import org.scalatest.{ Matchers, WordSpec }

import scala.collection.immutable.Seq
object test {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(273); val res$0 = 
  planner getPathsAtTime (munich, frankfurt, ice726MunichTime);System.out.println("""res0: Set[List[com.typesafe.training.scalatrain.Hop]] = """ + $show(res$0))}
}
